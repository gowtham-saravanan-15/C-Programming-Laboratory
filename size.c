#include<stdio.h>
int main()
{
    printf("The size of integer is:%dbytes\n",sizeof(int));
    printf("The size of character is:%dbytes\n",sizeof(char));
    printf("The size of double is:%dbytes\n",sizeof(double));
    printf("The size of float is:%d bytes\n",sizeof(float));
    return 0;
}
